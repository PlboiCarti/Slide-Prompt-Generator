# URL gốc của backend API (FastAPI). Vite dev server đã proxy /api sang
# http://localhost:8000 (xem vite.config.ts) nên giá trị dưới đây phù hợp
# cho cả dev và khi build production trỏ trực tiếp tới backend.
VITE_API_URL=http://localhost:8000/api
